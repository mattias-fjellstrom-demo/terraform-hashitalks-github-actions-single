variables {
  location            = "{{LOCATION}}"
  name_suffix         = "hashitalks-${{LOCATION}}"
}

provider "azurerm" {
  features {}
}

run "setup" {
  module {
    source  = "app.terraform.io/mattias-fjellstrom/resource-group-module/hashitalks"
    version = "{{VERSION}}"
  }
}
